Example of using `vite-plugin-ssr` with Mithril w/server-side routing.

```bash
git clone git@github.com:pep108/vite-plugin-ssr-mithril-server-routing
cd vite-plugin-ssr-mithril-server-routing
yarn install
yarn dev
```
